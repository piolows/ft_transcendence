services:
  frontend:
    image: frontend
    container_name: frontend
    volumes:
      - front_end:/app
    build: ./frontend
    restart: on-failure
    ports:
      - "4116:4116"
    networks:
      - transcendence
  backend:
    image: backend
    container_name: backend
    volumes:
      - back_end:/app
    build: ./backend
    restart: on-failure
    ports:
      - "4161:4161"
    networks:
      - transcendence

volumes:
  front_end:
    name: front_end
    driver: local
    driver_opts:
      type: none
      device: ${PWD}/frontend/app
      o: bind
  back_end:
    name: back_end
    driver: local
    driver_opts:
      type: none
      device: ${PWD}/backend/app
      o: bind

networks:
  transcendence:
    name: transcendence
    driver: bridge
